# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
.claude/settings.local.json

# sentry — kan inneholde auth-token i klartekst (.env* dekker
# .env.sentry-build-plugin, men ikke denne)
.sentryclirc

# MERK: qk-docs-temp/ og QK_*-filer er BEVISST IKKE ignorert (9. september
# 2026). QK_0–QK_7 skal kun finnes i claude.ai-prosjektet, og en lokal kopi
# som gjenoppstår skal være SYNLIG som «??» i git status så den kan fjernes.
# Ignorering ville gjort gjenoppståelsen stum — som er nettopp slik
# qk-docs-temp/ fikk ligge og bli fasit i tre uker. Se .claude/CLAUDE.md,
# «Dokumentregler». Ikke legg inn et ignore-mønster her «for ryddighet».
